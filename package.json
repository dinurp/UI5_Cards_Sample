{
  "name": "ui5_cards_sample",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "scripts": {
    "dev":   "npx http-server webapp -a 127.0.0.1 -c-1 -d false -o /",
    "build": "npx ui5 build",
    "start": "npx http-server dist -a 127.0.0.1 -d false -o /",
    "test": "grunt unit_and_integration_tests"
  }
}
